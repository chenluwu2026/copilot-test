# 复制到仓库根目录为 .env（服务器上 /opt/aims/copilot-test/.env）
# docker compose 会自动读取用于变量替换

# 公网访问时改成你的 IP 或域名（逗号分隔）
# CORS_ORIGINS=http://1.2.3.4:8080,http://你的域名:8080
# RUN_SEED=true
# DATA_PROVIDER=akshare
# API_KEY=change-me
